#Our model supposes that squared threshold contrast is given by:
# c^2 = a + b*(integrated noise power), where a and b are constants

fitNoiseIntegrationModel <- function(integrationRadius,noiseCheckSizeDeg,decayRadii,noiseSD,userThresholds){
  
  
  
}
